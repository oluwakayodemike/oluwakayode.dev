---
interface Props {
  id?: string;
}

const { id } = Astro.props;
---

<h2 id={id} data-section-header class="mdx-heading mdx-heading-2">
  <a href={id ? `#${id}` : undefined} class="mdx-heading-link">
    <slot />
  </a>
</h2>
